"use client";

import { Canvas } from "@react-three/fiber";

export default function Main({ children }: any) {
  return (
    <div>
      {/* <Canvas>{children}</Canvas> */}
      {children}
    </div>
  );
}
